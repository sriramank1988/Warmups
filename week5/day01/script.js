function sortNumber(a, b) {
    return a - b;
}
var numArray = [140000, 104, 99, 77, 55, 5, 4];
numArray.sort(sortNumber);

console.log(numArray);
numArray.forEach()

var pushPerfectSquare = function (n) {
    return n > 0 && Math.sqrt(n) % 1 === 0;
};